apiVersion: v1
kind: Service
metadata:
  labels:
    project: piggymetrics
    tier: backend
    app: auth-service
  name: auth-service
spec:
  ports:
  - name: "5000"
    port: 5000
    targetPort: 5000
  selector:
    project: piggymetrics
    tier: backend
    app: auth-service
status:
  loadBalancer: {}
